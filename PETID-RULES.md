# Правила переделки Admin → PetID

## 🚫 ЧТО НЕ ТРОГАЕМ

### 1. Порты
- **Backend**: остается 9000 (не меняем на 8100!)
- **Frontend**: остается 4000
- Все порты остаются как в текущей конфигурации

### 2. База данных
- **Удаленная PostgreSQL**: 88.218.121.213:5432
- Подключение через Gateway API (как сейчас)
- Данные хранятся в БД, здесь только отображаются
- Не создаем собственное подключение к БД

### 3. Gateway
- **URL**: https://api.zooplatforma.ru
- Архитектура остается: Frontend → Backend → Gateway → Database
- Backend остается прокси-сервером к Gateway

### 4. Авторизация
- JWT через cookies
- Middleware остается без изменений
- Проверка токенов через Gateway

### 5. Docker и деплой
- docker-compose.yml не трогаем
- Dockerfile не трогаем
- Переменные окружения (.env) не трогаем

### 6. Технологии
- Go backend остается
- Next.js 16 frontend остается
- Все зависимости остаются

## ✅ ЧТО МЕНЯЕМ

### ТОЛЬКО FRONTEND!

Backend уже умеет проксировать любые запросы к Gateway - его НЕ ТРОГАЕМ вообще!

### 1. Пункты меню (Frontend навигация)
Меняем только sidebar в `frontend/app/(dashboard)/layout.tsx`:

**Убираем:**
- Users (Пользователи)
- Posts (Посты)
- Organizations (Организации)
- Moderation (Модерация)

**Добавляем:**
- Pets (Питомцы)
- Species (Виды животных)
- Breeds (Породы)
- Medical Records (Медицинские карты)
- Documents (Документы)

**Оставляем:**
- Dashboard (главная)
- Logs (логи)
- Monitoring (мониторинг)
- Health (здоровье системы)

### 2. Страницы (Frontend pages)
Создаем новые папки в `frontend/app/(dashboard)/`:
- `/pets` - список питомцев
- `/species` - справочник видов
- `/breeds` - справочник пород
- `/medical-records` - медицинские карты
- `/documents` - документы

Каждая страница просто отображает данные из Gateway через существующие компоненты (TableWidget, StatsWidget).

### 3. API клиент (lib/api.ts)
Добавляем новые методы для работы с питомцами:
```typescript
getPets: () => apiClient.get('/api/admin/pets'),
getSpecies: () => apiClient.get('/api/admin/species'),
getBreeds: () => apiClient.get('/api/admin/breeds'),
// и т.д.
```

### 4. Названия и тексты
- "ЗооАдминка" → "PetID"
- "Административная панель" → "База данных питомцев"
- Все тексты в интерфейсе

## 📝 Важные замечания

1. **Данные в БД**: Все данные уже есть в удаленной PostgreSQL, мы только делаем интерфейс для их отображения
2. **Gateway API**: Все запросы идут через Gateway, который знает как работать с БД
3. **Прокси-архитектура**: Backend остается простым прокси без бизнес-логики
4. **Переиспользование**: Максимально используем существующий код, меняем только UI и роуты

## 🎯 План работы

1. Изменить названия в документации (README, ADMIN.md → PETID.md)
2. Переименовать пункты меню в frontend
3. Создать новые страницы для питомцев
4. Добавить новые handlers в backend
5. Обновить API клиент (lib/api.ts)
6. Протестировать все endpoints

---

**Дата создания**: 9 февраля 2026
**Статус**: Правила зафиксированы, готовы к работе
