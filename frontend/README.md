# ЗооПлатформа - Кабинет зоопомощника

Кабинет для волонтёров и зоопомощников платформы ЗооПлатформа.

## Архитектура

```
Volunteer Frontend (4000) → Volunteer Backend (9000) → Gateway (80) → Main Service / Other Services
```

## Установка

```bash
cd frontend
npm install
```

## Конфигурация

Создайте файл `.env.local`:

```env
# Admin Backend API
NEXT_PUBLIC_API_URL=http://localhost:9000

# Gateway API (для прямых запросов)
NEXT_PUBLIC_GATEWAY_URL=http://localhost:80

# Environment
NEXT_PUBLIC_ENVIRONMENT=development
```

## Запуск

### Разработка

```bash
npm run dev
```

Админ-панель будет доступна на `http://localhost:4000`

### Production

```bash
npm run build
npm start
```

## Функционал

### 📊 Дашборд
- Общее количество пользователей
- Пользователи онлайн
- Верифицированные пользователи
- Организации

### 👥 Пользователи
- Просмотр всех пользователей
- Детальная информация о пользователе
- Блокировка/разблокировка
- Удаление пользователей

### 📝 Посты
- Просмотр всех постов
- Модерация постов (одобрение/отклонение)
- Удаление постов

### 🏢 Организации
- Просмотр всех организаций
- Верификация приютов, клиник, фондов
- Отклонение заявок
- Статистика по организациям

### 📋 Логирование
- Просмотр действий администраторов
- Фильтрация по типу действия
- История изменений

### 🔍 Мониторинг
- Последние ошибки системы
- Системные метрики
- Статистика ошибок по сервисам
- Health check всех сервисов

### 🚨 Модерация
- Просмотр жалоб пользователей
- Рассмотрение жалоб
- Статистика модерации

## Авторизация

Админ-панель использует SSO через cookie `auth_token` от Gateway.

### Для локальной разработки:

1. Запустите Gateway на порту 80
2. Войдите через Gateway (`http://localhost:80/api/auth/login`)
3. Gateway установит cookie `auth_token`
4. Откройте админ-панель `http://localhost:4000`
5. Панель автоматически проверит токен

### Требования:

- Пользователь должен иметь роль `superadmin`
- Cookie `auth_token` должен быть валидным
- Gateway должен быть доступен

## Технологии

- **Next.js 16** - React фреймворк
- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Tailwind CSS 4** - стилизация
- **Heroicons** - иконки
- **@pet/shared** - общие компоненты

## Структура проекта

```
frontend/
├── app/
│   ├── (dashboard)/          # Защищенные страницы
│   │   ├── dashboard/        # Главная страница
│   │   ├── users/            # Управление пользователями
│   │   ├── posts/            # Управление постами
│   │   ├── organizations/    # Управление организациями
│   │   ├── logs/             # Логи
│   │   ├── monitoring/       # Мониторинг
│   │   ├── moderation/       # Модерация
│   │   └── layout.tsx        # Layout с навигацией
│   ├── auth/                 # Страница авторизации
│   ├── components/           # Компоненты
│   │   ├── admin/            # Админ компоненты
│   │   └── widgets/          # Виджеты
│   └── lib/                  # Утилиты
│       └── api.ts            # API клиент
├── public/                   # Статические файлы
└── package.json
```

## API Клиент

Используйте `apiClient` из `lib/api.ts`:

```typescript
import { apiClient } from '@/lib/api';

// GET запрос
const response = await apiClient.get('/api/admin/users');

// POST запрос
const response = await apiClient.post('/api/admin/users', { name: 'John' });

// PUT запрос
const response = await apiClient.put('/api/admin/users/1', { name: 'Jane' });

// DELETE запрос
const response = await apiClient.delete('/api/admin/users/1');
```

Все запросы автоматически включают:
- `credentials: 'include'` - для отправки cookies
- `Content-Type: application/json`
- Обработку ошибок

## Разработка

### Добавление новой страницы

1. Создайте папку в `app/(dashboard)/`
2. Добавьте `page.tsx`
3. Добавьте роут в `layout.tsx`

### Добавление нового компонента

1. Создайте файл в `app/components/`
2. Экспортируйте компонент
3. Используйте в страницах

### Стилизация

Используйте Tailwind CSS классы:

```tsx
<div className="bg-white rounded-lg shadow-sm p-6">
  <h1 className="text-2xl font-bold text-gray-900">Title</h1>
</div>
```

## Troubleshooting

### Ошибка "Не авторизован"

1. Проверьте что Gateway запущен
2. Войдите через Gateway
3. Проверьте что cookie `auth_token` установлен
4. Проверьте что у пользователя есть роль `superadmin`

### Ошибка "Доступ запрещен"

Убедитесь что у пользователя есть роль `superadmin`.

### Gateway недоступен

Проверьте что Gateway запущен на порту 80.

### Admin Backend недоступен

Проверьте что Admin Backend запущен на порту 9000.

## Production

### Build

```bash
npm run build
```

### Deploy

1. Соберите проект
2. Загрузите на сервер
3. Настройте переменные окружения
4. Запустите `npm start`

### Environment Variables

```env
NEXT_PUBLIC_API_URL=https://admin-api.zooplatforma.ru
NEXT_PUBLIC_GATEWAY_URL=https://api.zooplatforma.ru
NEXT_PUBLIC_ENVIRONMENT=production
```

## Лицензия

Proprietary - ЗооПлатформа

