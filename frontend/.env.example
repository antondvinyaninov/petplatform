# Volunteer Cabinet Frontend Configuration Example

# ============================================
# SERVER-SIDE VARIABLES (Next.js API routes)
# ============================================

# Backend API URL (internal, not exposed to browser)
# Development: http://localhost:9000
# Production: http://volunteer-backend:9000 (Docker internal network)
ADMIN_API_URL=http://localhost:9000

# ============================================
# CLIENT-SIDE VARIABLES (exposed to browser)
# ============================================

# Backend API (for direct browser requests)
# Development: http://localhost:9000
# Production: empty string (use relative paths via Next.js rewrites)
NEXT_PUBLIC_API_URL=http://localhost:9000

# Gateway API URL
# Both dev and prod: https://api.zooplatforma.ru
NEXT_PUBLIC_GATEWAY_URL=https://api.zooplatforma.ru

# Environment
# development | production
NEXT_PUBLIC_ENVIRONMENT=development
