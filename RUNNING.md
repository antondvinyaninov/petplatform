# ✅ Админ-панель запущена!

## 🚀 Статус сервисов

- ✅ **Admin Backend:** http://localhost:9000 (запущен)
- ✅ **Admin Frontend:** http://localhost:4000 (запущен)
- ✅ **Gateway:** https://api.zooplatforma.ru (работает)

## 📋 Что делать дальше

### 1. Авторизуйтесь

**Вариант A: Через главный сайт (проще)**

1. Откройте в браузере: https://zooplatforma.ru
2. Войдите под пользователем с ролью `superadmin`
3. После входа откройте: http://localhost:4000
4. Админ-панель автоматически проверит ваш токен

**Вариант B: Скопировать cookie вручную**

1. Войдите на https://zooplatforma.ru
2. Откройте DevTools (F12)
3. Application → Cookies → https://zooplatforma.ru
4. Найдите `auth_token` и скопируйте значение
5. Откройте http://localhost:4000
6. В DevTools создайте cookie:
   - Name: `auth_token`
   - Value: (вставьте)
   - Domain: `localhost`
   - Path: `/`
7. Обновите страницу

### 2. Проверьте работу

Откройте: http://localhost:4000

Вы должны увидеть:
- ✅ Дашборд с статистикой
- ✅ Меню навигации слева
- ✅ Данные пользователей и организаций

### 3. Попробуйте разделы

- **Дашборд** - общая статистика
- **Пользователи** - управление пользователями
- **Посты** - модерация постов
- **Организации** - верификация организаций
- **Логи** - действия администраторов
- **Мониторинг** - ошибки и метрики
- **Модерация** - жалобы пользователей

## 🔍 Проверка

### Проверьте Backend

```bash
curl http://localhost:9000/api/admin/health
```

Должен вернуть:
```json
{"status": "ok", "service": "admin-api"}
```

### Проверьте Gateway

```bash
curl https://api.zooplatforma.ru/health
```

Должен вернуть:
```json
{"status":"healthy","success":true,...}
```

### Проверьте авторизацию (с cookie)

```bash
curl http://localhost:9000/api/admin/auth/me \
  -H "Cookie: auth_token=YOUR_TOKEN"
```

Должен вернуть данные пользователя с ролью `superadmin`.

## 🐛 Если что-то не работает

### "Не авторизован"

1. Войдите на https://zooplatforma.ru
2. Проверьте что cookie `auth_token` установлен
3. Скопируйте cookie на localhost

### "Доступ запрещен"

У пользователя нет роли `superadmin`. Проверьте:

```bash
curl https://api.zooplatforma.ru/api/auth/me \
  -H "Cookie: auth_token=YOUR_TOKEN"
```

В ответе должно быть: `"roles": ["user", "superadmin"]`

### CORS ошибки

Проверьте что в `backend/.env` есть:
```env
CORS_ORIGINS=http://localhost:4000,http://localhost:3000,https://api.zooplatforma.ru
```

### Страница не загружается

1. Проверьте что backend запущен: `curl http://localhost:9000/api/admin/health`
2. Проверьте что frontend запущен: `curl http://localhost:4000`
3. Посмотрите логи в терминале

## 📝 Логи

### Backend логи

Смотрите в терминале где запущен backend (процесс 2)

### Frontend логи

Смотрите в терминале где запущен frontend (процесс 4)

### Browser DevTools

Откройте DevTools (F12) → Console для ошибок JavaScript

## 🛑 Остановка сервисов

### Остановить Backend

```bash
# Найдите процесс
ps aux | grep "go run main.go"

# Остановите
kill <PID>
```

### Остановить Frontend

```bash
# Найдите процесс
ps aux | grep "next dev"

# Остановите
kill <PID>
```

Или просто нажмите `Ctrl+C` в терминале где запущен сервис.

## 🔗 Полезные ссылки

- **Admin Panel:** http://localhost:4000
- **Admin API:** http://localhost:9000
- **Gateway:** https://api.zooplatforma.ru
- **Main Site:** https://zooplatforma.ru

## 📚 Документация

- [README.md](README.md) - общая документация
- [LOCAL_SETUP.md](LOCAL_SETUP.md) - детальная настройка
- [QUICKSTART.md](QUICKSTART.md) - быстрый старт
- [ARCHITECTURE.md](ARCHITECTURE.md) - архитектура
- [backend/README.md](backend/README.md) - backend API
- [frontend/README.md](frontend/README.md) - frontend

## 💡 Советы

1. **Используйте разные браузеры** для main site и admin panel
2. **Держите DevTools открытым** чтобы видеть ошибки
3. **Проверяйте Network tab** чтобы видеть запросы
4. **Смотрите логи** в терминале

---

**Готово!** Админ-панель запущена и готова к работе 🎉

Откройте http://localhost:4000 и начните работу!
