# 🚀 Админ-панель готова к работе!

## ✅ Что запущено

- ✅ **Admin Backend:** http://localhost:9000
- ✅ **Admin Frontend:** http://localhost:4000  
- ✅ **Gateway:** https://api.zooplatforma.ru

## 🔐 Как войти

### Откройте админ-панель

1. Откройте в браузере: **http://localhost:4000**
2. Вы увидите форму входа
3. Введите email и пароль пользователя с ролью `superadmin`
4. Нажмите "Войти"

### Что происходит при входе

1. Frontend отправляет запрос на `https://api.zooplatforma.ru/api/auth/login`
2. Gateway проверяет credentials и создаёт JWT токен
3. Gateway устанавливает cookie `auth_token`
4. Frontend проверяет что у пользователя есть роль `superadmin`
5. Если всё ОК - редирект на дашборд

## ❌ Если не получается войти

### Ошибка "Неверный email или пароль"

- Проверьте правильность email и пароля
- Убедитесь что пользователь существует в базе

### Ошибка "Требуются права суперадмина"

- У пользователя нет роли `superadmin`
- Нужно добавить роль в базе данных

### Ошибка "Ошибка подключения к серверу"

Проверьте что Gateway доступен:

```bash
curl https://api.zooplatforma.ru/health
```

Должен вернуть:
```json
{"status":"healthy","success":true,...}
```

## 🎯 После входа

Вы увидите:

- **Дашборд** - статистика платформы
- **Пользователи** - управление пользователями
- **Посты** - модерация контента
- **Организации** - верификация организаций
- **Логи** - действия администраторов
- **Мониторинг** - ошибки и метрики
- **Модерация** - жалобы пользователей

## 🔧 Проверка работы

### 1. Backend работает?

```bash
curl http://localhost:9000/api/admin/health
```

Ответ: `{"status": "ok", "service": "admin-api"}`

### 2. Frontend работает?

Откройте: http://localhost:4000

Должна загрузиться страница входа.

### 3. Gateway работает?

```bash
curl https://api.zooplatforma.ru/health
```

Ответ: `{"status":"healthy","success":true,...}`

## 📝 Тестовый вход (через curl)

```bash
# Войдите
curl -X POST https://api.zooplatforma.ru/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"your@email.com","password":"yourpassword"}' \
  -c cookies.txt

# Проверьте токен
curl https://api.zooplatforma.ru/api/auth/me \
  -b cookies.txt

# Должен вернуть данные пользователя с roles: ["user", "superadmin"]
```

## 🐛 Отладка

### Смотрите логи Backend

```bash
# Логи в терминале где запущен backend
# Или используйте Kiro для просмотра процесса 2
```

### Смотрите логи Frontend

```bash
# Логи в терминале где запущен frontend
# Или используйте Kiro для просмотра процесса 5
```

### Смотрите Browser Console

1. Откройте http://localhost:4000
2. Нажмите F12 (DevTools)
3. Перейдите в Console
4. Попробуйте войти
5. Смотрите ошибки в консоли

### Смотрите Network Tab

1. Откройте DevTools (F12)
2. Перейдите в Network
3. Попробуйте войти
4. Смотрите запросы к Gateway
5. Проверьте что cookie `auth_token` установлен

## 🎉 Готово!

Теперь вы можете:

1. ✅ Войти в админ-панель
2. ✅ Управлять пользователями
3. ✅ Модерировать контент
4. ✅ Верифицировать организации
5. ✅ Просматривать логи
6. ✅ Мониторить систему

## 📚 Дополнительная документация

- [README.md](README.md) - общая документация
- [ARCHITECTURE.md](ARCHITECTURE.md) - архитектура системы
- [LOCAL_SETUP.md](LOCAL_SETUP.md) - детальная настройка
- [backend/README.md](backend/README.md) - backend API
- [frontend/README.md](frontend/README.md) - frontend

## 💡 Полезные ссылки

- **Admin Panel:** http://localhost:4000
- **Admin API:** http://localhost:9000
- **Gateway:** https://api.zooplatforma.ru
- **Main Site:** https://zooplatforma.ru

---

**Всё готово! Откройте http://localhost:4000 и войдите!** 🚀
